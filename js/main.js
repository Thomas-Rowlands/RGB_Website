$(function () {
  $('#room-colour-picker').colorpicker({
    component: '.btn',
    container: "#room-colour-picker",
    format: "rgb"
  });
  $('#greg-colour-picker').colorpicker({
    component: '.btn',
    container: "#greg-colour-picker",
    format: "rgb"
  });
  $('#patrick-colour-picker').colorpicker({
    component: '.btn',
    container: "#patrick-colour-picker",
    format: "rgb"
  });
  $('#rini-colour-picker').colorpicker({
    component: '.btn',
    container: "#rini-colour-picker",
    format: "rgb"
  });
  $('#thomas-colour-picker').colorpicker({
    component: '.btn',
    container: "#thomas-colour-picker",
    format: "rgb"
  });
});
